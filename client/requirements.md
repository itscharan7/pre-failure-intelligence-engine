## Packages
(none needed)

## Notes
Uses existing installed recharts for time-series charts
Uses existing installed framer-motion for micro-interactions + page entry
All API calls use @shared/routes (api + buildUrl) and validate with Zod safeParse + console error logging
No auth flows implemented (API appears session-cookie ready via credentials: "include")
